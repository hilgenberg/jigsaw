#pragma once
#include "Utility/Vector.h"

typedef P2d PuzzleCoords; // here each piece has w=h=1
typedef P2d CameraCoords; // space that the camera looks into (same as puzzle, but scaled by sx,sy)
typedef P2d ScreenCoords; // screen pixels (what the camera projects into)
